{
    "group_whitelist": {
        "description": "群组白名单",
        "hint": "仅在白名单里的群启用本插件功能，不填则表示所有群都启用。",
        "type": "list",
        "default": []
    },
    "admin_users": {
        "description": "管理员QQ列表",
        "hint": "只有在此列表中的用户才有权限使用 /删除, /备份群文件 等管理指令。为了安全，强烈建议配置此项！",
        "type": "list",
        "default": []
    },
    "preview_length": {
        "description": "文本预览长度 (字符)",
        "hint": "使用 /搜索 文件名 <序号> 指令时，显示的内容预览的最大字符数。",
        "type": "int",
        "default": 1000
    },
    "default_zip_password": {
        "description": "默认ZIP解压密码",
        "hint": "在预览加密的ZIP压缩包时，会尝试使用此密码进行解压。",
        "type": "string",
        "default": ""
    },
    "storage_limits": {
        "description": "群文件容量监控阈值 (列表)",
        "hint": "格式为 \"群号:文件数量上限:空间上限GB\"，例如 \"123456:1000:9.5\"。当任一数值达到上限时，上传文件会触发提醒。",
        "type": "list",
        "default": []
    },
    "scheduled_check_tasks": {
        "description": "定时失效文件检查任务 (列表)",
        "hint": "格式为 \"群号:cron表达式\"。例如 \"123456:0 3 * * 1\" 代表为群123456在每周一凌晨3点执行一次检查。",
        "type": "list",
        "default": []
    },
    "scheduled_autodelete": {
        "description": "定时任务自动删除失效文件",
        "hint": "开启后，定时任务检测到失效文件将直接删除，而不是只发送报告。默认只报告，不删除。",
        "type": "bool",
        "default": false
    },
    "backup_zip_password": {
        "description": "备份压缩包加密密码",
        "hint": "使用 /gfb 指令备份文件时，生成的ZIP压缩包将使用此密码加密。留空则不加密。",
        "type": "string",
        "default": ""
    },
    "backup_file_size_limit_mb": {
        "description": "单文件备份大小上限 (MB)",
        "hint": "超过此大小的群文件将跳过备份。设置为 0 或不填则表示无限制。",
        "type": "int",
        "default": 100
    },
    "backup_file_extensions": {
        "description": "允许备份的文件扩展名",
        "hint": "只有在列表中的文件类型才会被备份。用逗号分隔，如: txt,pdf,jpg",
        "type": "text",
        "default": "txt,zip" 
    }
}